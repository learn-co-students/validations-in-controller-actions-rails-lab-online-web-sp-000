#class CategoryValidator < ActiveModel::Validator
#  def validate(record)
#    unless record == "Fiction" || record == "Non-Fiction"
#      record.errors[:category] << "is not a valid size!"
#    end
#  end
#end
